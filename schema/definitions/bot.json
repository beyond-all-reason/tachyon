{
    "$id": "bot",
    "type": "object",
    "properties": {
        "hostUserId": {
            "format": "uuid",
            "description": "UserId of the player hosting this AI",
            "type": "string"
        },
        "name": {
            "type": "string"
        },
        "aiShortName": {
            "type": "string"
        },
        "aiVersion": {
            "type": "string"
        },
        "customOptions": {
            "type": "object",
            "patternProperties": {
                "^(.*)$": {}
            }
        }
    },
    "required": [
        "hostUserId",
        "name",
        "aiShortName",
        "aiVersion",
        "customOptions"
    ],
    "title": "Bot"
}