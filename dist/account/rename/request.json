{
    "$id": "account/rename/request",
    "requiresLogin": false,
    "type": "object",
    "properties": {
        "messageId": {
            "type": "string"
        },
        "commandId": {
            "const": "account/rename/request",
            "type": "string"
        },
        "data": {
            "type": "object",
            "properties": {
                "newUsername": {
                    "minLength": 3,
                    "maxLength": 20,
                    "pattern": "^[A-Za-z0-9_-]+$",
                    "examples": [
                        "Bob"
                    ],
                    "type": "string"
                }
            },
            "required": [
                "newUsername"
            ]
        }
    },
    "required": [
        "messageId",
        "commandId",
        "data"
    ]
}